#!/usr/bin/env bash

# Update the package list and install Nginx with automatic "yes" answers to prompts
sudo apt-get update
sudo apt-get install -y nginx

# Create a simple HTML file with "Hello World!" content
echo "Hello World!" | sudo tee /var/www/html/index.html

# Configure Nginx to listen on port 80 with a custom configuration
sudo bash -c 'cat <<EOF > /etc/nginx/sites-available/default
server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html;
    index index.html;

    location / {
        try_files \$uri \$uri/ =404;
    }
}
EOF'

# Restart Nginx without using systemctl
sudo /etc/init.d/nginx restart

# Display a message indicating the setup is complete
echo "Nginx has been installed and configured to respond with 'Hello World!' at the root directory."

